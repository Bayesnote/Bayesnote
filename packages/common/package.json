{
  "name": "@bayesnote/common",
  "version": "0.0.1",
  "dependencies": {
    "uuid": "^8.0.0"
  },
  "scripts": {
    "watch": "del ./lib/**/* && tsc --build ./tsconfig.json -w",
    "build": "del ./lib/**/* && tsc --build ./tsconfig.json"
  },
  "devDependencies": {
    "@types/uuid": "^7.0.3",
    "del-cli": "^3.0.0",
    "typescript": "^3.8.3"
  }
}
